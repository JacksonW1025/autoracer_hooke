--- a/src/autoware/core/localization/autoware_ekf_localizer/include/autoware/ekf_localizer/measurement.hpp
+++ b/src/autoware/core/localization/autoware_ekf_localizer/include/autoware/ekf_localizer/measurement.hpp
@@ -36,6 +36,9 @@
   const std::array<double, 36ul> & covariance, const size_t smoothing_step);
 Eigen::Matrix2d position_only_pose_measurement_covariance(
   const std::array<double, 36ul> & covariance, const size_t smoothing_step);
+Eigen::Matrix2d position_only_pose_innovation_covariance(
+  const Eigen::MatrixXd & state_covariance, bool include_gnss_bias,
+  const std::array<double, 36ul> & measurement_covariance);
 Eigen::Matrix2d twist_measurement_covariance(
   const std::array<double, 36ul> & covariance, const size_t smoothing_step);
 
--- a/src/autoware/core/localization/autoware_ekf_localizer/src/measurement.cpp
+++ b/src/autoware/core/localization/autoware_ekf_localizer/src/measurement.cpp
@@ -106,6 +106,17 @@
   return r * static_cast<double>(smoothing_step);
 }
 
+Eigen::Matrix2d position_only_pose_innovation_covariance(
+  const Eigen::MatrixXd & state_covariance, const bool include_gnss_bias,
+  const std::array<double, 36ul> & measurement_covariance)
+{
+  const auto h =
+    position_only_pose_measurement_matrix(state_covariance.rows(), include_gnss_bias);
+  const Eigen::Matrix2d r =
+    position_only_pose_measurement_covariance(measurement_covariance, 1);
+  return h * state_covariance * h.transpose() + r;
+}
+
 Eigen::Matrix2d twist_measurement_covariance(
   const std::array<double, 36ul> & covariance, const size_t smoothing_step)
 {
--- a/src/autoware/core/localization/autoware_ekf_localizer/src/ekf_module.cpp
+++ b/src/autoware/core/localization/autoware_ekf_localizer/src/ekf_module.cpp
@@ -28,6 +28,8 @@
 #include <tf2/LinearMath/Quaternion.hpp>
 #include <tf2/utils.hpp>
 
+#include <Eigen/Cholesky>
+
 #include <fmt/core.h>
 
 #include <algorithm>
@@ -462,12 +464,28 @@
       gate_threshold = std::sqrt(params_.position_only_nis_gate);
     }
   } else if (is_position_only_measurement) {
-    const Eigen::MatrixXd c_gate = position_only_pose_measurement_matrix(
-        dim_x_, params_.enable_gnss_bias_estimation);
     const Eigen::Vector2d y_ekf_position = y_ekf.head<2>();
     const Eigen::Vector2d y_position = y.block<2, 1>(0, 0);
-    const Eigen::Matrix2d p_position = c_gate * p_curr * c_gate.transpose();
-    distance = mahalanobis(y_ekf_position, y_position, p_position);
+    const Eigen::Matrix2d innovation_covariance =
+        position_only_pose_innovation_covariance(
+            p_curr, params_.enable_gnss_bias_estimation,
+            pose.pose.covariance);
+    const Eigen::LDLT<Eigen::Matrix2d> covariance_ldlt(
+        innovation_covariance.selfadjointView<Eigen::Lower>());
+    if (!innovation_covariance.allFinite() ||
+        covariance_ldlt.info() != Eigen::Success ||
+        (covariance_ldlt.vectorD().array() <= 0.0).any()) {
+      warning_->warn(
+          "[EKF] invalid position-only innovation covariance. Ignore update.");
+      return false;
+    }
+    distance = std::sqrt(
+        std::max(squared_mahalanobis(
+                     y_ekf_position, y_position, innovation_covariance),
+                 0.0));
+    if (params_.position_only_nis_gate > 0.0) {
+      gate_threshold = std::sqrt(params_.position_only_nis_gate);
+    }
   } else {
     const Eigen::MatrixXd c_gate = pose_measurement_matrix(dim_x_);
     const Eigen::Matrix3d p_y = c_gate * p_curr * c_gate.transpose();
@@ -477,7 +495,7 @@
       std::max(distance, pose_diag_info.mahalanobis_distance);
   if (distance > gate_threshold) {
     pose_diag_info.is_passed_mahalanobis_gate = false;
-    if (use_dual_projection) {
+    if (is_position_only_measurement) {
       ++position_only_nis_rejected_count_;
     }
     warning_->warn_throttle(
@@ -561,7 +579,7 @@
     warning_->warn("[EKF] pose measurement update failed. ignore update.");
     return false;
   }
-  if (use_dual_projection) {
+  if (is_position_only_measurement) {
     const int64_t source_stamp_ns =
         rclcpp::Time(pose.header.stamp).nanoseconds();
     if (!has_accepted_position_only_measurement_ ||
--- a/src/autoware/core/localization/autoware_ekf_localizer/test/test_measurement.cpp
+++ b/src/autoware/core/localization/autoware_ekf_localizer/test/test_measurement.cpp
@@ -131,6 +131,32 @@
   EXPECT_EQ((m - expected).norm(), 0);
 }
 
+TEST(
+  Measurement,
+  position_only_innovation_covariance_includes_state_and_receiver_uncertainty)
+{
+  Eigen::Matrix<double, 6, 6> state_covariance =
+    Eigen::Matrix<double, 6, 6>::Zero();
+  state_covariance(0, 0) = 0.04;
+  state_covariance(0, 1) = 0.01;
+  state_covariance(1, 0) = 0.01;
+  state_covariance(1, 1) = 0.09;
+
+  std::array<double, 36> measurement_covariance{};
+  measurement_covariance.fill(0.0);
+  measurement_covariance[0] = 0.16;
+  measurement_covariance[1] = 0.02;
+  measurement_covariance[6] = 0.02;
+  measurement_covariance[7] = 0.25;
+
+  const Eigen::Matrix2d innovation_covariance =
+    position_only_pose_innovation_covariance(
+      state_covariance, false, measurement_covariance);
+  Eigen::Matrix2d expected;
+  expected << 0.20, 0.03, 0.03, 0.34;
+  EXPECT_NEAR((innovation_covariance - expected).norm(), 0.0, 1.0e-15);
+}
+
 TEST(Measurement, twist_measurement_covariance)
 {
   {
--- a/src/autoware/core/localization/autoware_ekf_localizer/schema/sub/pose_measurement.sub_schema.json
+++ b/src/autoware/core/localization/autoware_ekf_localizer/schema/sub/pose_measurement.sub_schema.json
@@ -81,7 +81,7 @@
         "position_only_nis_gate": {
           "type": "number",
           "minimum": 0.0,
-          "description": "Maximum two-dimensional single-sample NIS for dual-projection updates; zero disables this dedicated gate.",
+          "description": "Maximum two-dimensional single-sample NIS for every explicitly marked position-only update; zero falls back to pose_gate_dist.",
           "default": 0.0
         },
         "position_only_gnss_minimum_xy_std_m": {
--- a/src/autoware/universe/localization/autoware_pose_covariance_modifier/src/pose_covariance_modifier.cpp
+++ b/src/autoware/universe/localization/autoware_pose_covariance_modifier/src/pose_covariance_modifier.cpp
@@ -363,6 +363,22 @@
       return false;
     }
   }
+  const double covariance_xy = msg.pose.covariance[1];
+  const double covariance_yx = msg.pose.covariance[6];
+  const double covariance_scale = std::max(
+    {1.0, std::abs(msg.pose.covariance[X_POS_IDX_]),
+     std::abs(msg.pose.covariance[Y_POS_IDX_])});
+  if (std::abs(covariance_xy - covariance_yx) > 1.0e-9 * covariance_scale) {
+    RCLCPP_WARN(this->get_logger(), "%s XY covariance is not symmetric", source);
+    return false;
+  }
+  const double horizontal_determinant =
+    msg.pose.covariance[X_POS_IDX_] * msg.pose.covariance[Y_POS_IDX_] -
+    covariance_xy * covariance_yx;
+  if (!std::isfinite(horizontal_determinant) || horizontal_determinant <= 0.0) {
+    RCLCPP_WARN(this->get_logger(), "%s XY covariance is not positive definite", source);
+    return false;
+  }
   return true;
 }
 
--- a/src/autoware/universe/localization/autoware_pose_covariance_modifier/test/test_pose_covariance_modifier.cpp
+++ b/src/autoware/universe/localization/autoware_pose_covariance_modifier/test/test_pose_covariance_modifier.cpp
@@ -258,6 +258,25 @@
   }
 }
 
+TEST(PoseCovarianceModifier, ndt_primary_rejects_invalid_horizontal_covariance_matrix)
+{
+  ModifierHarness harness("ndt_primary_position");
+  harness.publish_ndt(harness.make_message(0.15));
+  harness.clear();
+
+  auto asymmetric = harness.make_message(0.2);
+  asymmetric.pose.covariance[1] = 0.01;
+  asymmetric.pose.covariance[6] = 0.0;
+  harness.publish_gnss(asymmetric);
+  EXPECT_TRUE(harness.outputs().empty());
+
+  auto indefinite = harness.make_message(0.2);
+  indefinite.pose.covariance[1] = 0.05;
+  indefinite.pose.covariance[6] = 0.05;
+  harness.publish_gnss(indefinite);
+  EXPECT_TRUE(harness.outputs().empty());
+}
+
 TEST(PoseCovarianceModifier, ndt_primary_position_keeps_xy_and_removes_inertial_yaw)
 {
   ModifierHarness harness("ndt_primary_position");
