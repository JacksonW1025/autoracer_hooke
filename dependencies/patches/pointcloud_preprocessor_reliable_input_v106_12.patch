--- a/src/autoware/universe/sensing/autoware_pointcloud_preprocessor/include/autoware/pointcloud_preprocessor/filter.hpp
+++ b/src/autoware/universe/sensing/autoware_pointcloud_preprocessor/include/autoware/pointcloud_preprocessor/filter.hpp
@@ -239,6 +239,14 @@
   /** \brief The maximum queue size (default: 3). */
   size_t max_queue_size_ = 3;
 
+  /** \brief Request reliable delivery for the input subscription.
+   *
+   * The default remains false to preserve Autoware's SensorDataQoS behavior
+   * for physical sensors.  Deterministic local simulators may opt in when a
+   * reliable publisher and sufficient processing headroom are both proven.
+   */
+  bool input_qos_reliable_ = false;
+
   /** \brief True if we use an approximate time synchronizer
    * versus an exact one (false by default). */
   bool approximate_sync_ = false;
--- a/src/autoware/universe/sensing/autoware_pointcloud_preprocessor/src/filter.cpp
+++ b/src/autoware/universe/sensing/autoware_pointcloud_preprocessor/src/filter.cpp
@@ -73,6 +73,7 @@
     tf_input_frame_ = static_cast<std::string>(declare_parameter("input_frame", ""));
     tf_output_frame_ = static_cast<std::string>(declare_parameter("output_frame", ""));
     max_queue_size_ = static_cast<std::size_t>(declare_parameter("max_queue_size", 5));
+    input_qos_reliable_ = declare_parameter<bool>("input_qos_reliable", false);
 
     // ---[ Optional parameters
     use_indices_ = static_cast<bool>(declare_parameter("use_indices", false));
@@ -88,6 +89,8 @@
         << " - approximate_sync : " << (approximate_sync_ ? "true" : "false") << std::endl
         << " - use_indices      : " << (use_indices_ ? "true" : "false") << std::endl
         << " - latched_indices  : " << (latched_indices_ ? "true" : "false") << std::endl
+        << " - input qos        : " << (input_qos_reliable_ ? "reliable" : "best_effort")
+        << std::endl
         << " - max_queue_size   : " << max_queue_size_);
   }
 
@@ -134,11 +137,15 @@
   auto callback = supported_nodes.find(filter_name) != supported_nodes.end()
                     ? &Filter::faster_input_indices_callback
                     : &Filter::input_indices_callback;
+  auto input_qos = rclcpp::SensorDataQoS().keep_last(max_queue_size_);
+  if (input_qos_reliable_) {
+    input_qos.reliable();
+  }
 
   if (use_indices_) {
     // Subscribe to the input using a filter
     sub_input_filter_.subscribe(
-      this, "input", rclcpp::SensorDataQoS().keep_last(max_queue_size_).get_rmw_qos_profile());
+      this, "input", input_qos.get_rmw_qos_profile());
     sub_indices_filter_.subscribe(
       this, "indices", rclcpp::SensorDataQoS().keep_last(max_queue_size_).get_rmw_qos_profile());
 
@@ -159,7 +166,7 @@
     std::function<void(const PointCloud2ConstPtr msg)> cb =
       std::bind(callback, this, std::placeholders::_1, PointIndicesConstPtr());
     sub_input_ = create_subscription<PointCloud2>(
-      "input", rclcpp::SensorDataQoS().keep_last(max_queue_size_), cb);
+      "input", input_qos, cb);
   }
 }
