/**:
  ros__parameters:
    sample_num: 1500
    # -1 preserves upstream behavior. Validation may override this to make
    # GNSS on/off A/B runs consume byte-identical NDT point subsets.
    deterministic_seed: -1
